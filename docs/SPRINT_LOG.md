# Sprint Log — xposeTIP v1.2.0

## Sprint history

| Sprint | Key deliverables |
|--------|-----------------|
| 1-5 | Core: Docker, auth, Holehe, HIBP, Sherlock, score engine, graph, timeline, settings |
| 6-10 | Premium scanners, OAuth framework, intelligence engine, fingerprint, scraper engine |
| 11-15 | Identity estimation, persona clustering, dual score, real-time logs, multi-workspace |
| 16-20 | OAuth accounts, 43 scrapers, scraper UI, plans, registration |
| 21-25 | Admin panel, name blacklist, DNS blocklist, executive summary, CSV export |
| 26-30 | 51 scrapers, PageRank, eigenvalue fingerprint, generative avatar, landing page, SSE |
| 31-35 | 71 scrapers, pixel art avatar, quick scan, landing redesign |
| 36-40 | Markov chain, graph_context, name resolution, email age fix, graph connectivity |
| 41-42 | Clustering fix, persona platform, bio cleanup, accounts tab, graph readability |
| 43 | CLAUDE.md rewrite, architecture whitepaper, roadmap |
| 44-50 | 85 scrapers, corporate scrapers, public exposure axis (9th), enhanced fingerprint |
| 51-60 | 100 scrapers, scanner expansion (35 modules), L5 premium tier, Pass 2 name enrichment |
| 61-70 | 117 scrapers, Pass 1.5 username expansion, 3-pass pipeline, identity-first narrative |
| 71 | Component extraction: Landing.jsx and Architecture.jsx split into focused sub-components |
| 72 | Narrative rewrite: identity-first landing + architecture ("From IOCs to identities") |
| 73 | Bug Fix Blitz: pre-flush truncation, import alias fix, WHOIS JSON safety, scan pagination, table scroll, geo map pins, username validator |
| 73b | Username filter: is_valid_username() applied in username_expander + profile_aggregator |
| 73c | Avatar quality ranking (_score_avatar), always-sync to target.avatar_url |
| 73d | CLAUDE.md split into 4 files, version bump to v0.73.0 |
| 74 | PDF identity report export (ReportLab, dark theme, 5-page Pro tier, tiered by plan) |
| 75 | Avatar quality gate: _score_avatar >= 2 for avatar_url, GenerativeAvatar fallback, PhotosTab quality badges, Dashboard Quick Scan removed |
| 76 | Deep Username Scan: POST /targets/{id}/scan-username, scan_single_username (300s/80 scrapers), Celery task + lightweight refinalize, UI button with polling + DEEP badge |
| 77 | Bug Fix Blitz: photos confidence %, timeline DNS date filter, username validator strengthened, UsernameTab sort real>intelligence, severity-based analyzer confidence |
| 78 | User Dashboard Preview (/user-preview): score hero + GenerativeAvatar 160px, evolution chart (Recharts), remediation checklist, GET /targets/{id}/remediation endpoint |
| 79 | Code Leak & Paste Monitoring: 3 new scrapers (GitHub Code Search email+username, GitHub Gists), psbdmp re-enabled, CodeLeakAnalyzer (10 sensitive patterns), code_leak category |
| 80 | Account Depth Profiling: enriched extraction on 5 scrapers (+19 fields), BehavioralProfiler analyzer (5 archetypes + longevity + high-activity detection) |
| 81 | Full docs & landing update to v0.80.0: CLAUDE.md, SPRINT_LOG.md, ARCHITECTURE.md, API_REFERENCE.md |
| 82 | Generic Deep Indicator Scan: POST /targets/{id}/scan-indicator (username/email/domain/name), Deep Scan button on all findings |
| 83 | The xpose Manifesto: /manifesto page, 3 pillars (Ethical OSINT, Green Intelligence, Education First), landing trust bar |
| 84 | CRITICAL FIX — Deep Scan Pipeline: _full_refinalize (15 steps replacing broken _lightweight_refinalize), cascade scan (cross-type indicator chaining), _store_result indicator_type fix |
| 85 | Docs, landing & version bump to v0.85.0 |
| 86 | Pipeline Reorder: two-phase Gather-then-Compute (Pass 1.5/2 before graph build, early profile bootstrap) |
| 87 | Bug Fix Blitz II: ratio-based risk levels, persona confidence rebalance, 429 exponential backoff retry |
| 88 | Graph username nodes fix (platform/username keys in _store_result), Deep Scan Activity panel in OverviewTab |
| 89 | CRITICAL — Scraper real module names (not scraper_engine), indicator_value fallback, dedup maps legacy findings |
| 90 | Docs, landing & architecture update to v0.90.0 |
| 91 | README rewrite: v0.90.0, two-phase pipeline, 120 scrapers, changelog |
| 92 | Manifesto v2: red lines, data commitment, B2B consent model, sourced green numbers |
| 93 | Extraction rules enrichment: 17 scrapers, ~68 new fields (bios, followers, dates, skills) |
| 94 | Geographic Intelligence: 132 countries, 123 cities, location normalization, url_last_segment transform |
| 95 | Timezone Intelligence: sleep window detection, UTC offset inference, 27 offset→region mappings |
| 96 | Geo Consistency Scoring: 6-signal cross-correlation (ground truth, self-reported, timezone, nationalize, language, geoip) |
| 97 | HOTFIX: Timeline wayback leak, profile perf (country_code param), email non-deliverable banner |
| 98 | Full documentation & landing alignment for Nexus (v0.98.0) |
| 98b | SCANNERS.md rewrite + architecture visual updates + demo script |
| 99 | Smarter Pass 2 queries (context from email domain + city) + photo quality tiers |
| 100 | v1.0.0: Persona confidence formula + tab labels + scan progress + MX lookup |
| 101 (A) | Discovery Engine: DB migration — 3 tables (discovery_sessions, discovery_leads, target_links) |
| 102 (B) | Discovery Engine: 6 extractors (rel_me 0.95, jsonld 0.95, social_link 0.85, email 0.90, meta_tag 0.80, username 0.60) |
| 103 (C) | Discovery Engine: Query generator (11 templates, 4 tiers) + variant engine + budget manager |
| 104 (D) | Discovery Engine: Fetch pipeline — SerpAPI → trafilatura → extractors → quality gate → DB |
| 104.5 | Discovery Engine: Relevance filter + nav/footer stripping |
| 104.6 | Discovery Engine: 8 quality fixes — LinkedIn penalty, geo, disambiguation, platform blocklist, dedup |
| 104.7 | Discovery Engine: Final quality — page relevance, common name blocklist, generic fallback removal |
| 105 (E) | Discovery Engine: Celery task + 3 API endpoints + 4 SSE event types |
| 106 (F) | Discovery Engine: Discovered tab — launch, lead cards, dismiss/undo, filter pills, chain view |
| 107-110 | Discovery UX polish chain (v1.1.1-v1.1.4): event trail + zombie guard + junk filter + EmailRep API key + avatar fix + PivotStrategy engine + multi-round pivot execution |
| 111-112 | Auto-ingest + Phase A.5 targeted rescan (v1.1.5) + stability hotfixes (v1.1.6: graph dedup, discovery timeout, junk filter) |
| 113-114 | Comprehensive hotfix (v1.1.7: timeouts, UI, email_age, tabs) + module cleanup (v1.1.8: 9 phantoms removed/disabled) |
| 115-117 | Secondary identifier pipeline (v1.1.9: finalize_scan A1.5/A1.6 steps) + Phone + crypto scrapers (v1.1.10: 6 sources seeded disabled, key-based JSONB extraction) + OSS readiness (v1.1.11: LICENSE flip to AGPL-3.0, CLA infra, gitleaks audit clean) |
| 118 | 4-tier SaaS alignment (v1.1.12): consultant→starter plan rename, new Team tier, Alembic migration 012, frontend planColors shared module |
| 119 | Courtlistener (v1.1.13): US federal court scraper MVP, RECAP archive via REST API v4, token auth, legal_record indicator_type |
| 120 | EU legal scrapers (v1.1.14): BODACC (FR procédures collectives) + UK Gazette (London/Edinburgh/Belfast) |
| S119-S120 | Risk Signals UI block on Overview (v1.1.15) + Findings tab preset filter chips with shared `lib/findingFilters.js` (v1.1.16) |
| S121 + S121b | Doc drift closure post-S116→S120: stale landing JSX refs, DEMO_SCRIPT pricing table aligned with S113 4-tier |
| S122 chain (S122a → S122e-holdover) | Per-scraper attempt logging in `attempt_log` JSONB, `name_scraper_engine` factored out, aggregator tie-break (Gravatar email-verified beats heuristic), emoji regex extended to U+2600 block, 2 Gravatar duplicates disabled, pastebin_user HTML extraction fixed, low-confidence severity for placeholder name-scrapers |
| S123 | Holdover rollback + courtlistener defensive: PASS2 int→str coerce for indicator_value, courtlistener RECAP shape variant parse |
| S124-S126 | Verified provenance UI per finding (tier badges + cross-verif + first_seen/last_seen timeline) + Recalculate Profiles retro-rebuild + match_confidence row in ProvenanceCard |
| S127-S129 | name_scraper_engine sequential dispatch post-A3 + telemetry rectification (scraper_engine returns (found, reason), no_data classification for explicit_not_found/blocked_403) + Pass 1 username dispatch gated by is_valid_username (HN-style 400 fix) |
| S130 | Doc closure + v1.2.0 stamp: scripts/seed_scrapers.py audit (127/110/17/11 cats), SPRINT_LOG backfill S121-S130, DEMO_SCRIPT archived as Nexus v1 + rewritten dual Play 1/Play 2 narrative |
| S131 | DB-persisted similarity engine: Alembic 013 (`target_similarities` table), 9-axis cosine threshold 0.70, `first_detected` preserved across recomputes, Celery task post scan.completed, `GET /targets/{id}/similar`, `SimilarityBlock.jsx` on Overview tab |
| S132 | Landing refresh + Compare page: 6-card FeaturesSection, TwoWaysSection (Play 1 purple / Play 2 green), `/compare` route + Compare.jsx (tri-segment matrix + SVG quadrant) |
| S133 | TopoJSON geomap: world-atlas 110m, D3 Natural Earth, country-density heatmap, zoom/pan 1-8×, `lib/geo.js` (useWorldData/useZoom), `topojson-client` dep |
| S134 | R4 cleanup combo: `_clean_name_value` regex tightening + Courtlistener defensive cast + NEW `scans.cascade_state` (Alembic 014, gathering/computing/similarity/done/failed) + cascade stepper UI |
| S135 | UI polish 4-pack: backend always-non-null fingerprint_avatar_seed/axes, `TargetDetail` post-`done` re-fetch kills cascade race, fallbackSeed aligned across 4 consumers |
| S136 | Glyph in header dual-avatar (80px photo + 32px glyph badge) + Fingerprint Evolution scroll cue (right/left fades + drag hint + scroll-snap) |
| S137 | Cleanup combo: centralized `fallbackSeed` → `dashboard/src/lib/avatar.js`, NEW `docs/qa/SMOKE_PROTOCOL.md` |
| S138 | Public nav + footer unification: NEW `PublicNav.jsx` + `PublicFooter.jsx` shared across 4 public pages, active state via `useLocation()`, deleted `LandingFooter.jsx` + `ArchFooter.jsx`, `ArchCTA` extracted, `/changelog` link pre-added |
| S139 | Pricing tweaks: dropped `price` field from Starter/Team/Enterprise, Free keeps "€0 forever", `PricingSection` conditional render |
| S140 | Architecture full sexy refresh: 3 new stages (Cascade/Similarity/Discovery) + diagrams, hero stat banner 127·9·5.4B·11·0, NEW TechStackSection.jsx, ScraperBreakdown active/disabled split (110/17), RoadmapSection v1 backfill, CollectDiagram counts realigned (127) |
| S141 | `/changelog` public page parsed from git log: build-time `generate-changelog.mjs` via predev/prebuild hooks, 210 commits in JSON, filterable timeline grouped by month |
| S142 | Doc closure post-S141 — markdown reflects reality, sprint count badge updated, `/changelog` page noted, footer dashboard count reconciled (commit 035192b) |
| S143 | Fingerprint calibration diagnostic — pre-BFP audit. NEW `scripts/diag_fingerprint_calibration.py` ran 12 workspaces (159 FPs, 1968 pairs), proved 9-axis saturates 5/9 axes + 3 workspace-constants → cosine 0.88-1.00 intra-corpus. Justified S144 + S145+S147 (commit 87b1ce5 + 7 chore commits) |
| S144 | Data-driven AXIS_MAX recalibration + fingerprint recompute. `accounts` 15→60, `platforms` 10→50, `username_reuse` 5→10, `data_leaked` 8→25, `email_age_years` 15→40. NEW `scripts/recompute_fingerprints.py` (idempotent, `--dry-run`/`--limit`/`--workspace`). 172 targets recomputed, sim table 56→3586 backfill (commit 1a57162) |
| S145 | `formal_records` 10th fingerprint axis — routes Courtlistener/BODACC/UK Gazette via `indicator_type='legal_record'`, `FingerprintRadar.jsx` consumes dynamic `ALL_AXIS_LABELS` from backend, AXIS_MAX=5 (commit b9a21cd) |
| S146 | Name-aware similarity engine — `combined = cosine × name_sim`. NEW migration 015 (`name_similarity` + `cosine_similarity` cols), Unicode token-set Jaccard via stdlib `unicodedata`. `target_similarities` 3586→0 on first recompute (FP cleanup) (commit 7b4d907) |
| S147 | `network_signature` 11th fingerprint axis — spectral entropy of identity-graph eigenvalues. AXIS_MAX 1.0, SCORE_WEIGHTS rebalanced (data_leaked 0.18→0.13, network_signature 0.05). Closes same-name homonym failure mode (commit af81c14) |
| S148 | Lost-task fragility — `acks_late=True` + `reject_on_worker_lost=True` on `launch_scan`, Redis broker `visibility_timeout=7200`. NEW `api/tasks/watchdog.py` `sweep_orphan_scans` every 5min via `beat_schedule` (queued>30min, no-progress>20min, hard-cap>240min). Exposed by orphan scan `c48712fc` (nabz0r@gmail.com, May 20 worker restart storm) — first watchdog tick swept it via hard_timeout branch (commit dc0475a) |
| S149 | Polling cascade orphelin — `TargetDetail.jsx` polling guard now uses shared `isScanInProgress` predicate matching the wider display predicate (covers `status='completed' AND cascade_state ∈ {gathering,computing,similarity}`). Playwright validated 4-min cascade window with continuous polling (commit 4ae31ff) |
| S150 | uq_identity rollback on rescan — get-or-create both `Identity` and `IdentityLink` in `_create_pe_graph_edges`, `session.rollback()` in every except around flush. Re-classified P0→P2 (silent data loss, not crash — `_set_cascade_state` S134 rollback masked `PendingRollbackError`, lost Pass 2 Findings + graph edges) (commit 6112faf) |
| S151 | Investigated, no repro found (fingerprint_history "absent on some profiles" — 215/215 targets had populated history, likely backfilled by S144 recompute). Skipped, no code change |
| S152 | Live Scans cross-orgs admin tab + System stale-data bundle fix. NEW `GET /system/scans/live` + `POST /system/scans/{id}/cancel` (superadmin), NEW `SystemLiveScansTab.jsx` (polls 3s). Bundle fix: 6 System tabs `useEffect` deps gain `refreshKey` (commit ed6d269) |
| Smoke S148-S152 | Multi-layer Playwright validation on 3 targets across 2 workspaces. Verdict: PASS WITH NOTES. Surfaced S153 cancel race bug, S150 validation gap, /system/stats counter inconsistency. Evidence + report in `docs/qa/smoke_2026-05-22_s148_s149_s150_s152.md` (commit 05299a1) |
| S153 | Cancel race condition fix — chord child task revoke + finalize_scan guard. NEW `api/tasks/utils.py` helpers `stash_scan_child_tasks` + `revoke_scan_tasks`, Redis SADD `scan:{id}:child_tasks` TTL 24h at dispatch, both cancel endpoints iterate+revoke before status flip. Layer B killed chord callback before Layer A's early-return ever fired in validation (commit e2befba) |
| S154 | Workspace-aware refresh bundle + Redis close + system_stats scoping. Settings.jsx + Scrapers.jsx `useEffect` deps gain `refreshKey`, `scraper_progress` Redis client closed in `finally`, `/system/stats` table counts now workspace-scoped (targets/scans/findings/identities + users via UserWorkspace; modules stay global) (commit 1f7ade4) |
| S155 | Synthetic test for S150 `_create_pe_graph_edges` idempotency. NEW `tests/test_create_pe_graph_edges.py` — 3 integration tests against throwaway `_s155_smoke` workspace (fixture-managed cleanup): reuses existing Identity on duplicate value, second call doesn't duplicate link, new value still creates new Identity+link. All 3 PASS. Closes S152 smoke validation gap (commit a7c308b) |
| S156 | Doc closure sweep S142-S154 + v1.3.0 stamp. 9-axis → 11-axis across CLAUDE.md/README/DEMO_SCRIPT, 26 scanners → 27, sprints 141+ → 154+, version 1.2.0 → 1.3.0 (commit 964b3a8) |
| S157 | TargetDetail tab consolidation 13 → 5 with sub-pill nav. NEW FindingsHubTab/GraphHubTab/SourcesHubTab wrappers, compat shim `setActiveTabCompat`, connected-accounts useEffect re-wired to `activeTab==='sources'`. Sub-pills mirror S120 preset chip style (commits 3e57517 + cb4d1e1 verify) |
| S158 | `module_progress` dict-vs-string defensive handling. NEW `dashboard/src/lib/moduleProgress.js` (normalizeModuleStatus + formatModuleStatus). Fixed 4 sites: ScansTab crash + 3 silent correctness sites in TargetDetail (commits 3a13640 + 282424f verify) |
| S159 | Username taxonomy correction. Backend writer fix (`scraper_scanner.py` fallback + empty-value guard) + seed_scrapers explicit `identity_type` on 15 scrapers (6 domain + 9 email) + alembic 016 (relabel 936 + delete 2245 empty + tag 1684 intelligence rows as `platform='name_synthesis'`) + frontend allow-list defense in `UsernameTab.jsx` + recompute_fingerprints recalibrated 113/175 targets (commit 80acd69 + 804b33f verify) |
| S159b | Cosmetic chip fixes post-S159 verify. UsernameTab normalize regex strip separators; extractPlatform accepts both findings and graph nodes; PlatformIcon adds npm/pypi/kaggle + PLATFORM_ALIASES + shared resolveKey helper (commit 56a3783) |
| S160 | BFP public page MVP — NEW `/bfp` public route + `BFP.jsx` wrapper + 3 components (BFPHero / BFPFoundation / BFPRoadmap). Hero: Inversion statement + 4 attribute pills. Foundation: 4 pillar cards. Roadmap: 4 buckets (Shipped / Active / Next / Long-term) with color-coded dots. PublicNav adds `/bfp` link. Architecture / Cryptography / Subject Layer / Ethics / Status deferred to S161-S163. Version bumped v1.3.0 → v1.3.1 |
| S161 | BFP page Architecture + Cryptography sections — NEW `BFPArchitecture.jsx` (2-layer split xposeTIP cloud vs BFP local binary + 4 infrastructure parallels DNS/CT/MISP/BFP + Trust + Threat model cards) + NEW `BFPCryptography.jsx` (5-row PQC stack: MinHash/SHA-3-256, SPHINCS+/SLH-DSA, ML-DSA/Dilithium, ML-KEM/Kyber, Merkle — all NIST FIPS 203/204/205 since August 2024). `BFP.jsx` interleaves the new sections between Foundation and Roadmap with alternating `[#0a0a0f]`/`[#0d0d14]` backgrounds. Subject Layer / Ethics deferred to S162; Status section + hero visual swap to S163. Version bumped v1.3.1 → v1.3.2 |
| S162 | BFP page Subject Layer + Ethics sections — NEW `BFPSubjectLayer.jsx` (4-tier service grid Level I-IV with Free/Paid pill + Play 3a/3b badges on paid tiers; Takedown rendered as separate full-width purple-accented card with circular ⏻ icon, framed as legal safety valve not a product tier) + NEW `BFPEthics.jsx` (italic thesis block with `border-l-2 border-[#00ff88]` accent + cypherpunk/Brin/Right-to-Read lineage; 3-principle grid: Asymmetry is the harm / Empowerment over paternalism / Auditable by design; 2-card commitments grid: Takedown protocol + Auditable methodology). Page now 7 sections (Hero → Foundation → Architecture → Cryptography → Subject Layer → Ethics → Roadmap). Status section + hero visual swap deferred to S163. Version bumped v1.3.2 → v1.3.3 |
| S163 | BFP page Status section + Hero Inversion visual — NEW `BFPStatus.jsx` (6-stat reference impl snapshot: Version v1.3.4 / Sprints 163+ / Active scrapers 110/127 / Fingerprint axes 11 / License AGPL-3.0 / Crypto suite NIST 2024 PQC, pulse-dot live indicator + GitHub link) + NEW `BFPInversionVisual.jsx` (single-composite 280×220 SVG: subject 4×4 hash grid centered + BFP ring + 6 actor circles + dashed-in extraction + teal-out vision arrows with markerEnd arrowheads — injected into Hero between badge and h1). `BFP.jsx` inserts Status between Ethics and Roadmap with bg flip on Roadmap. Page now 8 sections (Hero → Foundation → Architecture → Cryptography → Subject Layer → Ethics → Status → Roadmap). **BFP public page complete.** Version bumped v1.3.3 → v1.3.4 |
| S181 | Doc sync — S178 through S180-L backfill. Pure doc sprint, zero code. The 4 prior code sprints (S178/S179/S180-G/S180-L) shipped under strict spec scope = no doc touch, accumulating 4 SPRINT_LOG.md entries + 4 patch-version bumps not propagated to CLAUDE.md, README.md, or `PublicFooter.jsx`. Backfilled: 4 new entries inserted at top of post-BFP cluster in `docs/SPRINT_LOG.md`; 4 new paragraphs at top of CLAUDE.md recent-sprints list; CLAUDE.md `## Current version` v1.6.8 → v1.6.13; README.md version badge v1.6.8 → v1.6.13; `PublicFooter.jsx` v1.6.2 → v1.6.13 (closes 4-version drift since S171 was the last code sprint to touch the footer). Zero migration, zero new dep, zero behavior change. Version v1.6.12 → v1.6.13 |
| S180-L | BFP substrate per-target UI exposure. The BFP trust substrate (behavioral_hash_v1 from S166, bfp_claims from S167, cross_verification_count from S168) was fully populated in the DB but invisible in the UI. Smoke pass confirmed `nabz0r@gmail.com` had 14 claims + 74.3% cross-verification + 2048-char behavioral hash, all hidden from the operator viewing the target's Overview tab. **T1** New endpoint `GET /api/v1/targets/{id}/bfp` returns a dossier: `behavioral_hash` (full + short prefix + present flag), `claims` (total + breakdown by type), `cross_verification` (verified/total + ratio). Auth-gated via existing `get_current_workspace` + `get_current_user`. Lazy fetch from Overview keeps target list fast (3 COUNT queries on indexed columns, sub-50ms typical). **T2** New component `dashboard/src/components/bfp/BFPSubstrateBlock.jsx`. 3-column layout matching `RiskSignalsBlock` aesthetic. Click-to-copy on the truncated hash. Silent fail on load error / missing substrate — Overview stays uncluttered. Colors: purple (#aa66ff) for hash, blue (#3388ff) for claims, green (#00ff88) for cross-verif. **T3** Mount on `OverviewTab.jsx` between Digital Fingerprint and Fingerprint Evolution blocks (natural neighbor — both about identity primitives). Verified shape on nabz0r: behavioral_hash present, 14 claims, 84/113 verified (ratio 0.7434). Auth gate: unauth `GET /api/v1/targets/{id}/bfp` → HTTP 403. Out of scope: Merkle inclusion proofs per-target (workspace-scoped in BFP_SPEC §17, requires future protocol work); claims editing / export (read-only display this sprint); deep link to /bfp public marketing page (different audience). Closes P5 ANOMALY from the smoke-pass session. Version v1.6.11 → v1.6.12 |
| S180-G | A1.5/A1.6 pipeline observability. Phase 1 validation (re-scan on the seeded test target `gnienlnaha@yahoo.fr`) confirmed the secondary-identifier dispatch architecture is sound: A1.6 fires, runs enabled phone/crypto scrapers, produced 2 findings (`google_phone_dork` + `blockchain_info_btc` with 62,943 real txs / 107 BTC on the Satoshi address). The 222 other corpus targets produce 0 findings because upstream scrapers don't surface phone/wallet patterns in their finding text (innocent corpus, not a bug). But the pipeline was silent: A1.5 logged nothing when extraction returned empty, A1.6 logged nothing when `profile_data` had no phones/wallets, and A1.6's per-scraper attempt outcomes (success / no_match / exception) were not persisted anywhere. "Ran but found nothing" was indistinguishable from "never ran" — and silently-swallowed scraper errors were invisible. **T1** `secondary_identifier_enricher.py`: per-scraper attempt log written to `scan.module_progress["secondary_enricher_attempts"]` (mirrors `scraper_scanner.py` S122-obs pattern). Values: `success` / `no_match` / `exception`. Unconditional summary log line replaces the created-only log: `A1.6: scrapers=N inputs=phone:N/crypto:N created=N attempts=N`. **T2** `secondary_identifier_enricher.py`: explicit `A1.6: skipped` log on the empty-inputs early-return path. **T3** `secondary_identifiers.py`: explicit `A1.5: no phone/crypto patterns matched in N findings` log on the no-extraction branch. Validation re-scan confirmed: chainabuse_check + blockchair_wallet returned `no_match` (not `exception`) on Satoshi's address — they ran cleanly without errors. No new dep. No migration. No behavior change to scraper execution — purely observability. Out of scope: S180-H findingFilters.js typos (cosmetic); S180-I "no exposure detected" UX hint; S180-J phone/crypto catalog expansion (Etherscan, Truecaller, Mempool.space, Tronscan, DeBank, Arkham, PACER, SEC EDGAR, OpenCorporates); S180-K manual-injection UI for Play 1 consulting workflow. Version v1.6.10 → v1.6.11 |
| S179 | Identities-table junk cleanup — validator overhaul + backfill. Two-way validator bug let ~290 junk rows accumulate in the identities graph table while simultaneously hiding 7 legitimate usernames. Claude Code's 45-case probe scored the existing `is_valid_username()` at 28/45 (62%): 10 false ACCEPTS (gmail.com / connection_quality / replit "handle (Full Name)" pattern) + 7 false REJECTS (`terry bailey`, `mini mike 111`, `sarah harding`, `nabilux ☃☃☃`, etc.). **T1** `username_validator.py`: (a) FQDN single-dot regex against TLD allow-list — rejects `gmail.com` but spares `josephine.lespierre`; (b) `connection_quality` added to `_TITLE_PATTERNS` as explicit known-offender blacklist (kept narrow rather than a general snake_case rule, which would have rejected `nabil_ksontini` — exactly the variant pattern this whole investigation started from); (c) `(` / `)` paren rejection for replit-style titles; (d) REMOVED the 2-word ≥3-char rule that caused 7 false rejects. Empirically redundant — real page titles in the corpus are all caught by other rules (3+ spaces, ` - `, `\|`, `_TITLE_PATTERNS`). **T2** `graph_builder.py`: chains the central validator into `_is_valid_extracted_username()` final return. Preserves all existing local rules (single-letter initials, `@` rejection, capitalized full-name detection) AND inherits central rules. **T3** Migration 022: per-platform narrowed DELETE filter, sanity count + 500-row safety cap, no-op downgrade (junk not recoverable). Filter spares ~230 false positives (Steam dotted handles, GitLab handles, chesscom display names). Live impact post-migration: 264 junk rows deleted (under 500 cap), 0 remaining, all 6 legit canaries preserved (josephine.lespierre / kamel.amroune / benjamin.lavault / mario.grotz / j.wood / marie-jo.gutenkauf). Telegram identities dropped 148 → 11 (137 "Telegram – a new era of messaging" page-title rows gone). Validator scorecard 28/45 → 18/18 (100%) on canary test set. Out of scope: 6 per-scraper source-of-truth fixes (S180-A through S180-F) — HTML title extraction bugs in aboutme/linktree/linkedin, HTML entity decoding in bandcamp, indicator_type misclassification in dns_* / hackertarget_* / rdap_* / wayback_* / crtsh_*, and the Telegram empty-profile detection. Version v1.6.9 → v1.6.10 |
| S178 | Username taxonomy visibility — intelligence-module findings surfaced. S159 + migration 016 hid 1,746 username findings (1,684 historic `name_synthesis`-tagged + 62 NULL-tagged ongoing) behind a frontend allow-list designed for scraper findings. Analyzer outputs from `username_correlator` carry their platforms in `data.platforms[]` (array, plural), not `data.platform` (scalar), so the `PLATFORM_COLORS` lookup in `UsernameTab.jsx:82` dropped them silently. Pair-correlation rows ("X and Y likely same person") also lost their second username because `indicator_value` only captures one endpoint. **T1** UsernameTab.jsx: dedicated intelligence-module branch via `_addIntelligenceFinding` helper. Bypasses PLATFORM_COLORS, expands `data.platforms[]` (normalized against the lookup table), surfaces `data.usernames[1]` for pair correlations, flags groups with `fromCorrelation=true`. Summary bar gains a correlation counter (purple chip). **T2** MODULE_PLATFORM_MAP: `github_deep → github`. Unblocks ~5 NULL-platform username rows from `github_scanner` per target. **T3** Migration 021: drops the now-redundant `data.platform='name_synthesis'` sentinel set by migration 016. Reversible; downgrade re-adds it. Live impact post-migration: 1,684 rows updated, 0 `name_synthesis` tags remain, 1,746 total intelligence/username rows now NULL-tagged + routed via the new branch. Functional verification via JSX simulation on `benjamin.lavault@kpmg.lu`: 23 hidden findings produced 7 username groups (benjamin / benjaminlavault / benjaminl / lavault / benjamin_lavault / lavaultbenjamin / blavault), 19 platform chips, 7 correlation flags — including `lavault` surfaced via `data.usernames[1]` pair-correlation extraction (previously invisible). Out of scope: Q3 identities-table junk (separate write path — landed in S179). Version v1.6.8 → v1.6.9 |
| S177 | BFP_SPEC Session 2 sub-C — §13 PQC stack specification. **End of Session 2; v0.2.x feature-stable.** Pure doc sprint, zero code. `docs/BFP_SPEC_v0.md` extended ~150-180 lines. **§13 restructured**: existing 3 generic bullets renamed §13.1 General Security Considerations (content unchanged). **§13.2 Post-Quantum Cryptography Stack NEW** — 9 sub-sections normative + informative. **§13.2.1 Use Cases** identifies 3 trust-layer crypto use cases (subject attestations / operator claim sigs / KEM for confidential channels). **§13.2.2 Algorithm Choices** locks NIST FIPS 205 (SLH-DSA for subject attestations), FIPS 204 (ML-DSA for operator sigs), FIPS 203 (ML-KEM for KEM) — protocol-level normative. Pre-quantum substitution FORBIDDEN. **§13.2.3 Parameter Sets** — SLH-DSA-128s/192s (s variants mandated for size), ML-DSA-44/65/87 (default 65), ML-KEM-768 (default and only mandated KEM param). **§13.2.4 Reference Library** — INFORMATIVE: liboqs-python as reference impl choice per `docs/specs/pqc_choices_v0.md` (S174), with documented fallbacks (pyspx for SLH-DSA, pyca for ML-DSA/ML-KEM, RustCrypto + AWS-LC alternates). Library choice declared OPERATIONAL, not protocol-level — preserves multi-language interop. NIST KAT verification REQUIRED before claiming conformance. **§13.2.5 Known Risks** documents R1 (no liboqs end-to-end audit, tracked in Appendix B), R2 (pyca encap tuple order divergence, mitigation = facade normalization), R3 (NIST errata, mitigation = signature_version). **§13.2.6 Sizes** — informative table for impl planning. Operational implication: ~11 KB sigs per claim row, ~17 GB/year trust-log overhead at S172 rate. **§13.2.7 Signature Format** — 6-field JSON object schema (alg / param_set / signature_version / key_id / signature base64url / signed_at RFC 3339). **§13.2.8 Versioning** — signature_version=1 = FIPS 203/204/205 published 2024-08. Old versions verifiable indefinitely. **§13.2.9 Conformance** — emission OPTIONAL in v0.x, REQUIRED for future cross-trust-boundary claim transfer (Appendix B item). Forward refs RESOLVED: §3.1 terms (Subject signature, Operator signature) drop "specified in v0.2.3" parenthetical → point to §13.2; §14.1 signature_version row updated from "§13 (v0.2.3)" → "§13.2". Doc top: **version bumped v0.2.2 → v0.2.3**, intro paragraph rewritten in past tense to mark Session 2 complete, closing marker updated to "end of Session 2, v0.2.x feature-stable". Reference impl version v1.6.7 → v1.6.8 (patch — doc-only). Zero code, zero migration, zero new dep. Session 2 complete: Session 1 (v0.2.0, S170) + Session 2 sub-A (v0.2.1, S175) + Session 2 sub-B (v0.2.2, S176) + Session 2 sub-C (v0.2.3, S177) = full protocol surface for v0.2.x |
| S176 | BFP_SPEC Session 2 sub-B — axis status flags + behavioral hash specification. Pure doc sprint, zero code. `docs/BFP_SPEC_v0.md` extended ~100-130 lines. **§5 intro (§5.0 NEW)** adds LIVE/DRAFT taxonomy explanation. **All 16 axes in §5** now carry a `**Status**:` bullet (inserted as 2nd item after `**Layer**:`): 11 LIVE + 5 DRAFT. LIVE: account_count, platform_diversity, geo_dispersion, media_presence, breach_incidents, breach_severity, email_security_posture, formal_records, email_age, handle_persistence, network_signature. DRAFT: account_hygiene, temporal_persistence, activity_rhythm, linguistic_signature, adversarial_indicators. **§9.3 Behavioral Hash NEW** — 9 sub-sections (Purpose / Selected Axes / Bucket Encoding / MinHash Parameters / Set Semantics / Output Format / Composition Property / Versioning / Conformance) locking the S166 implementation as normative MUST: 3 axes (media_presence, geo_dispersion, breach_severity), 20 buckets per axis, MinHash 128 permutations seed=42 (datasketch v1.x algorithm reference), element encoding `"{axis_name}:{bucket}"` ASCII bytes, missing-axis omission (NOT zero-default), empty-set → empty string → NULL persistence, output 1024 bytes / 2048 hex chars little-endian native byte order. Interop primitive: behavioral hashes MUST be byte-identical across compliant operators for same axis inputs. Composition property explicitly documented: behavioral hash is clustering NOT uniqueness; uniqueness via composition with subject signature + network-layer signals. `behavioral_hash_version = 1` tied into §14.1 component versioning table. **§11.1 collateral cleanup** — stale ref to "three v0-deferred axes" fixed: replaced with reference to §5 DRAFT flag, list of 5 DRAFTs documented; redundant `**Notes**:` lines removed from 5.3.3 (activity_rhythm), 5.4.2 (linguistic_signature), 5.5.1 (network_signature — was STALE since axis is now LIVE post-S147). New §11.1 MAY clause: implementations MAY compute behavioral hash per §9.3 (RECOMMENDED for cross-operator clustering). Doc top-of-doc: **version bumped v0.2.1 → v0.2.2**, date + intro paragraph + closing marker updated. §14 first line "This is v0.2.1" → "This is v0.2.2". Reference impl version v1.6.6 → v1.6.7 (patch — doc-only). Zero code, zero migration, zero new dep. S177 (sub-C: §13 PQC stack citing `pqc_choices_v0.md` → v0.2.3) sequenced for follow-up |
| S175 | BFP_SPEC Session 2 sub-A — trust-layer formalization. Pure doc sprint, zero code. `docs/BFP_SPEC_v0.md` extended ~150-200 lines across 6 sections: **§3 Terminology** restructured into Fingerprint-layer (v0.1+) and Trust-layer (v0.2+) sub-groups; 13 NEW terms added (Subject, Operator, Reference impl, Behavioral hash, Trust boundary, Trust log, Claim, Cross-verification source, Anchor, Merkle leaf, Merkle root, Subject signature, Operator signature). **§4.5 Trust Layer Model** NEW sub-section with 2-layer architecture (Layer 1 Fingerprint Computation specified in §4-§9; Layer 2 Trust + Anchoring specified in §13-§17) + ASCII diagram showing composition direction (findings + scores feed UP from Layer 1 into Layer 2). **§11 Conformance** restructured: existing 7 MUST + 3 MAY renamed §11.1 Fingerprint Layer Conformance unchanged; NEW §11.2 Trust Layer Conformance with 6 MUST (cross-verification per §15, claim emission per §16 with locked rule, Merkle anchor per §17 at least on demand, independent verification means, append-only preservation, single trust boundary scope) + 4 MAY (signatures per §13, inclusion proofs, supersession, §12.1 tiers). Trust-layer conformance requires fingerprint conformance underneath. **§12.1 Subject-Rights Tiers** NEW sub-section: 4-tier normative structure (Read / Guidance / Monitoring / Managed Remediation) + Takedown as legally-constrained soupape only (no discretionary takedown — would erode §16.7 append-only). Protocol-neutral framing locked: tiers normative as structure, pricing/packaging declared OPERATIONAL concern outside protocol. **§14.1 Component Versioning** NEW sub-section with 4-row table tying claim_hash_version (§16.3), root_version (§17.8), behavioral_hash_version (§9.3 forthcoming), signature_version (§13 forthcoming) to bfp_version. Decoupled-but-coordinated semantics: minor `bfp_version` bumps introduce new component versions; old components MUST remain interpretable. **Appendix B** refresh: reorganized into Fingerprint-layer / Trust-layer / Cross-cutting; shipped items moved to "moved out of deferred" (graph signature post-S147 + basic trust layer post-S166-S170); 9 NEW open items added (PQC claim emission, subject portal, monitoring infra, managed remediation workflow, inclusion proofs, claim supersession, multi-op federation, liboqs end-to-end audit, trust-layer conformance test suite). Doc top-of-doc: **version bumped v0.2.0 → v0.2.1**, intro paragraph extended to announce 3-session v0.2.x roadmap (Session 1 done v0.2.0 / Session 2 sub-A now v0.2.1 / Session 2 sub-B v0.2.2 / Session 2 sub-C v0.2.3). Closing marker `*End of BFP v0.2.0...*` → `v0.2.1`. Reference impl version v1.6.5 → v1.6.6 (patch — doc-only). Zero code, zero migration, zero new dep. S176 (sub-B: §5 axis status flags + §9.3 behavioral hash spec) and S177 (sub-C: §13 PQC stack citing `pqc_choices_v0.md`) sequenced for follow-up |
| S174 | PRE-S169 PQC library evaluation — NEW `docs/specs/pqc_choices_v0.md` (companion to BFP_SPEC §13, citable from future Session 2 PQC sub-section). Pure R&D sprint — zero code committed, zero migration, zero new dep in `requirements.txt`. NEW `docs/specs/` directory established for spec-companion docs (distinct from `docs/qa/` audits + `docs/diag/` data dumps). 6 candidate libraries evaluated against the FIPS 203/204/205 stack: liboqs-python (`oqs`), pyspx, pqcrypto, cryptography+OpenSSL+oqs-provider, RustCrypto via pyo3, AWS-LC via ctypes. 6 benchmark cells per library: SLH-DSA-128s + SLH-DSA-192s (subject attestations, low-volume, `s` variants for sig size); ML-DSA-44 + ML-DSA-65 + ML-DSA-87 (operator/scraper sigs, all 3 NIST param sets); ML-KEM-768 (NIST cat 3 default). **Major finding:** pyca cryptography 48.0.0 ships **native** `mldsa` + `mlkem` modules backed by Rust+OpenSSL — no `oqs-provider` build needed (originally assumed); 4/6 cells covered with zero `liboqs` dep. 4 of 6 libs installed + benchmarked in sandbox (pqcrypto, pyspx, liboqs-python, cryptography); RustCrypto skipped (no Rust toolchain) and AWS-LC skipped (no official Python PQC bindings) — both documented from upstream sources. **Findings logged:** liboqs-python first-import triggers 268s liboqs auto-build that ignores existing system liboqs (install pain HIGH); pqcrypto uses non-standard `sign(sk, msg)` order + has no declared license (YELLOW for production); pyca's `encapsulate()` returns `(ss, ct)` opposite of liboqs's `(ct, ss)` (integration footgun); ML-KEM correctly returns implicit-rejection pseudo-random `ss` on tampered ct per FIPS 203 FO transform. **LOCKED CHOICES per algorithm:** `liboqs-python` PRIMARY for all 3 families (fastest 1.5–30× across every cell — AVX2-vectorized PQClean refs); `pyspx` FALLBACK for SLH-DSA (CC0, intrinsic constant-time, canonical SPHINCS+ ref impl); `cryptography` (pyca) FALLBACK for ML-DSA + ML-KEM (Cure53-audited, Apache/BSD, mainstream supply chain). Mixed-library fallback strategy: pyspx + pyca = 6/6 coverage without liboqs dep at 3–30× perf cost. Container impact preview in §4.4: Dockerfile pre-builds liboqs to avoid runtime build storm, est. +150MB image; fallback stack +80MB. Sandbox: macOS 26.3.1 / i5-1038NG7 / Python 3.11.0 / OpenSSL 3.6.1. **Closes action item flagged in `docs/qa/bfp_pre_impl_audit_2026-05-23.md` (S164).** Unblocks BFP_SPEC Session 2 §13 extension. Version v1.6.4 → v1.6.5 (patch — doc-only) |
| S173 | `/bfp` live trust log widget + SSE zombie cleanup. **Widget half:** NEW route `GET /api/v1/bfp/recent_anchors?limit=20` (1-100 range, default 20) appended to existing `api/routers/bfp.py` — PUBLIC, no auth, mirrors `/stats` cache pattern with independent module-level `_RECENT_CACHE` dict + 60s TTL. Cache holds top 100 rows always, response slices to requested limit (no per-limit fragmentation). Returns `{anchors: [{root_hash, num_leaves, computed_at}, ...]}` ordered by `computed_at DESC` — NO `workspace_id` field exposed (privacy + visual cleanliness). NEW `dashboard/src/components/bfp/BFPLiveLog.jsx` polls every 60s via raw `fetch()` (NOT shared `request()` which auto-redirects 401), CT-log-style monospace table: full 64-char SHA3-256 `root_hash` + `num_leaves` + absolute ISO UTC `computed_at`, `overflow-x-auto` for narrow-viewport horizontal scroll. AbortController + cancelled flag on unmount. `BFP.jsx` inserts new section between `Status` (dark) and `Roadmap` — Roadmap's Section className flipped to `bg-[#0d0d14]` to preserve alternation. Page now 9 sections. **Cleanup half:** `dashboard/src/hooks/useSSE.js` DELETED entirely (no-op since S30 commit `de285fe`, March 2026 — "SSE saturates API workers"). 5 zombie call sites scrubbed across 4 pages: `Dashboard.jsx` (import L11 + call L55), `Targets.jsx` (L8 + L71), `TargetDetail.jsx` (L10 + 4-line block L99-102), `tabs/SystemDashboardTab.jsx` (L5 + L14). `api/main.py` UNregisters the events router: `events` removed from `from api.routers import ...` line + `include_router(events.router, ...)` line DELETED. `api/routers/events.py` left untouched on disk per locked Option B — code preserved for a future SSE-proper sprint (re-arch worker pool + rate limit + connection cap). Zero migration, zero new dep, zero touch to scan/persist/claim-emission paths. Version v1.6.3 → v1.6.4 |
| S172 | Live Merkle rebuild Celery beat — closes the S171 loop where `/bfp` page counters were frozen at last-manual-rebuild state. NEW `api/tasks/bfp.py` with `@celery_app.task(name="api.tasks.bfp.rebuild_merkle_roots")` mirroring the watchdog pattern: sync session, calls existing `rebuild_all_workspaces(session)` (the SOLE writer to `bfp_merkle_roots` + `bfp_claims.merkle_position`, S169), commits, publishes per-workspace `bfp.merkle_root_committed` event on the new `bfp:events` Redis channel. Beat schedule: **300s** (matches `/api/v1/bfp/stats` cache TTL — one rhythm). Insert semantic: **always-insert** (CT-STH-style liveness — every tick appends one row per workspace-with-claims regardless of root_hash/num_leaves change vs previous tick). `api/services/event_bus.py` generalized: `publish_event(event_type, data, channel=CHANNEL)` adds optional `channel` kwarg defaulting to existing `xpose:events`, so all 10 existing callers (scan_orchestrator x3, web_discovery x7) keep working unchanged; NEW `BFP_CHANNEL = "bfp:events"` constant added next to the existing `CHANNEL` constant. NEW beat_schedule entry `rebuild-bfp-merkle-roots` + NEW `import api.tasks.bfp` line in `api/tasks/__init__.py`. Zero migration, zero new dep, zero touch to scan/persist/claim-emission paths. Future public BFP SSE consumer can subscribe to `bfp:events` for a live trust-log widget (no consumer exists yet — channel is publish-only at MVP). Volume note: 15 workspaces × 288 ticks/day = ~4,320 rows/day, ~1.58M rows/year on `bfp_merkle_roots`; retention/pruning is a future-sprint concern around ~10M rows. Version v1.6.2 → v1.6.3 |
| S171 | `/bfp` page Status section — 3 stale placeholders (Version `v1.3.4` / Sprints `163+` / Active scrapers `110/127`) replaced with 3 LIVE counts pulled from BFP substrate. NEW `api/routers/bfp.py` exposing public unauthenticated `GET /api/v1/bfp/stats` (no `Depends(...)` at router OR route level — explicit isolation). Returns 3 COUNT(*) aggregates platform-wide across all workspaces: `behavioral_hashes_computed` (from `targets.bfp_behavioral_hash_v1 IS NOT NULL`, S166), `trust_claims_logged` (from `bfp_claims`, S167), `merkle_roots_committed` (from `bfp_merkle_roots`, S169). 300s in-memory TTL cache per worker (`_CACHE` dict, `time.time()` clock) — protects DB from public-traffic spikes, acceptable staleness for displayed counts. Router registered in `api/main.py` (1-line import + 1-line `include_router` at `/api/v1/bfp` prefix). FULL REWRITE of `dashboard/src/components/bfp/BFPStatus.jsx`: const `STATS` array removed, `useState` + `useEffect` introduced, raw `fetch()` used (NOT shared `request()` which auto-redirects 401 → /login and would hijack public visitors), em-dash `—` fallback on fetch failure (silent — no error banner on marketing page), `AbortController` on unmount. 3 protocol-constant statics kept (`Fingerprint axes: 11` / `License: AGPL-3.0` / `Crypto suite: NIST 2024 PQC`). Subhead "xposeTIP — May 2026" (date-stale) replaced with "xposeTIP — reference impl, live counts". Zero DB migration, zero new dependency, zero behavior change to scan/persist paths. Version v1.6.1 → v1.6.2 |
| S170 | `docs/BFP_SPEC_v0.md` committed — v0.2.0 public working draft, Apache-2.0. **Doc-only sprint** (zero code, zero migrations, zero behavior change). Session 1 of spec rewrite covers the trust layer formalization: **§2.3 entropy-honesty framing** (~28 bits Shannon → clustering primitive, not unique identifier; unique identity via composition with subject binding signature + network signals); **§15 Cross-Verification** (8 sub-sections: definition, source identity, subject- vs population-scoped, self-exclusion, idempotence, snapshot semantics, sources ordering, conformance); **§16 Claim Log** (9 sub-sections: 9-field claim structure + 4 reserved, JSON canonical encoding with sort_keys + `(",", ":")` separators, SHA-3-256 hash function, emission rule `cross_verification_count ≥ 1`, UNIQUE dedup, append-only, evidence-evolution deferral, conformance); **§17 Merkle Anchor** (12 sub-sections: per-trust-boundary scope, canonical leaf ordering `emitted_at` ASC + `claim_hash` tiebreaker, RFC 6962 leaf `SHA3-256(0x00 \|\| raw)` / internal `SHA3-256(0x01 \|\| left \|\| right)`, odd-leaf promotion with CVE-2012-2459 callout, empty-tree no-sentinel, anchor snapshot 5-field record, rebuild frequency unspecified, MUST provide independent verification means, conformance, explicit out-of-scope: inclusion proofs / consistency proofs / STH / gossip). Existing §15/§16 renamed to `Appendix A` / `Appendix B`. Closing marker `*End of BFP v0.2.0 public working draft.*`. Final file 877 lines (+338 vs prior internal v0.1.0). Non-normative notes reference the xposeTIP reference impl at v1.6.0 (`findings.cross_verification_count` 48.0%, 1,107 claims across 15 workspaces, 6 claim types). Closes the gap where the `/bfp` public page committed to a protocol with no published specification (the substrate from S166 + S168 + S167 + S169 is now publicly formalized). Sessions 2–3 deferred (§5 axes status flags, §9.2 MinHash spec, §11 conformance trust-layer extension, §13 PQC stack, §3 terminology, §4 2-layer diagram, §12 subject-rights tiers, §14 versioning ties, Appendix B refresh). PRE-S169 PQC eval spike now blocks Session 2 §13. Version v1.6.0 → v1.6.1 (patch — doc-only) |
| S169 | Merkle root over `bfp_claims` → tamper-evident trust log. Completes Phase 1 substrate. NEW `bfp_merkle_roots` table (Alembic 020): append-only per-workspace root snapshots (workspace_id/root_hash/num_leaves/root_version/computed_at; latest = MAX(computed_at)). NEW `api/services/bfp/merkle_builder.py`: **RFC 6962** binary Merkle tree, **SHA-3-256**, **`0x00 \|\| leaf` / `0x01 \|\| left \|\| right` domain separation** (defeats second-preimage at level boundaries), **odd-leaf promotion** (NOT duplication — CVE-2012-2459 antipattern). Canonical leaf order: `bfp_claims ORDER BY emitted_at ASC, claim_hash ASC` (deterministic, append-friendly). Populates `bfp_claims.merkle_position` (0-indexed, dense per workspace). NEW `scripts/build_bfp_merkle.py` (idempotent, `--workspace`/`--dry-run`); NEW `scripts/verify_bfp_merkle.py` (independent re-derivation from raw `claim_hash`, exit 1 on mismatch). **No app-code changes to existing write paths** (only `api/models/__init__.py` +2 lines registering the new model; `persist_scanner_results` untouched). Live: **15 workspaces, 1,107 claims → 15 roots** (sizes 6–297 leaves); `verify_bfp_merkle.py` 15/15 OK; second rebuild produces identical roots (stability proven); **tamper smoke: corrupted 1 hash → verify reports 14/15, exit 1; restored → 15/15, exit 0**. `bfp_claims.parent_hash` left NULL (reserved for inclusion-proof storage in a later sprint). Batch-only in MVP (no Celery beat hook yet, no API endpoint, no UI surface). Version v1.5.0 → v1.6.0 |
| S167 | `bfp_claims` append-only trust log — **first BFP protocol structure in the repo**. NEW `bfp_claims` table (Alembic 019, 16 columns incl. 4 reserved for S169/Phase 3) with UNIQUE `(target_id, claim_type, claim_value)`, content-addressable via SHA-3-256 `claim_hash` over canonical JSON (sort_keys + sorted sources + `claim_hash_version:1`). **Locked emission rule:** finding must have `cross_verification_count >= 1` AND non-null `indicator_type`+`indicator_value`. NEW `api/services/bfp/claim_emitter.py` (sole writer, `pg_insert.on_conflict_do_nothing` for idempotency). NEW `api/models/bfp_claim.py` (registered in `__init__`). NEW `scripts/backfill_bfp_claims.py` (idempotent). `persist_scanner_results` emits live alongside S168 cross-verif recompute, same transaction. Live: **1,107 claims emitted** from 8,012 cross-verified findings (ip 352 · username 301 · email 223 · domain 223 · first_name 6 · social_url 2). All 1,107 SHA-3-256 hashes valid 64-hex; recompute-from-stored verified deterministic. All 4 reserved columns NULL on every row. Append-only = convention only at MVP. Version v1.4.1 → v1.5.0 |
| S168 | Cross-verification formalization — BFP trust layer seed signal moved from read-time computation (`api/routers/findings.py:54-68` + `:154-167` S124-S126 lineage) to persisted columns. NEW `findings.cross_verification_count` (int) + `findings.cross_verification_sources` (jsonb) + `idx_findings_cross_verification_count` (Alembic 018). `persist_scanner_results` recomputes for all `(target_id, indicator_value)` tuples in each batch within the same transaction. `_finding_dict` reads from columns; response keys `cross_verified_count`/`cross_verified_by` preserved unchanged. NEW `scripts/backfill_cross_verification.py` (idempotent). Live: 223 targets / 16704 findings / 8012 backfilled updates (48% cross-verified), 1007/1007 consistency, dashboard unchanged. Trust-layer foundation for S167 `bfp_claims`. Version v1.4.0 → v1.4.1 |
| S166 | `bfp_behavioral_hash_v1` MVP — **first BFP-protocol code in repo**. NEW `FingerprintEngine._compute_behavioral_hash_v1()` (MinHash 128 perms seed=42, 20 buckets/axis, over `public_exposure`/`geo_spread`/`data_leaked` per S165 ranking). NEW `targets.bfp_behavioral_hash_v1` column + index (Alembic 017). NEW `scripts/recompute_bfp_behavioral_hash.py` (backfill + `--compare A B` Jaccard utility). Orchestrator persists from happy + Deep Scan paths. NEW `datasketch>=1.6,<2.0` dep. Wording: "Canonical identity hash" → "Canonical behavioral hash" with composition caveat (BFPArchitecture + BFPCryptography). Backfill: 223/223 targets (100%), 80 distinct hashes (143 collisions — expected at K=3 ≈ 6.68 bits entropy). Smoke: self-compare=1.0, distinct pairs 0.0–0.21. **Clustering primitive, not unique identifier.** Version v1.3.4 → v1.4.0 |
| S165 | BFP invariance diagnostic — NEW `scripts/bfp_invariance_diag.py` (read-only). Measures per-axis stability across `fingerprint_history` snapshots for 206 targets (avg 6.98 snaps/target). Outputs `docs/diag/invariance_2026-05-23.{md,json}`. Reports BOTH intra-target stability (mean_abs_delta, CV, range) AND inter-target discrimination (across-pop stdev, unique buckets) — canonical hash needs both. Recommendation: `public_exposure` / `geo_spread` / `data_leaked` are stable+discriminating. `security` is perfectly stable per-target but only 4 unique buckets across population. `network_signature` (S147) excellent on both, N=143. CLI: `--workspace` / `--limit` / `--output-dir` / `--date`. Substrate for S166 canonical hash axis selection |
| S164 | BFP pre-implementation audit — autonomous read/analysis sweep before BFP Phase 1 work. 7-section report at `docs/qa/bfp_pre_impl_audit_2026-05-23.md`: repo state (HEAD b529cd6, v1.3.4, 245-line sprint log) · platform inventory (127 scrapers / 27 scanners / 9 L4 analyzers / 11 axes + AXIS_MAX dump) · data corpus (15 workspaces / 223 targets / 16704 findings / 6113 identities; **206/223 targets have ≥2 fingerprint snapshots, avg 6.52 snapshots — strong invariance substrate**) · current fingerprint computation entry points (`fingerprint_engine.py:385` `FingerprintEngine.compute()` is the canonical insertion point; existing `_compute_hash` / `_compute_enhanced_hash` / `_compute_avatar_seed` are UI-only and don't block) · **22-row gap matrix BFP page promises vs code reality (15 hard gaps including MinHash/PQC/Merkle/subject portal/takedown, 5 partial gaps adaptable from `findings.verified` cross-verification infra at 41.8% coverage, 2 already-existing)** · tech debt (17 alembic migrations, 0 FIXMEs in fp/intelligence paths, 17 disabled scrapers, untracked BFP_SPEC stubs) · recommended Phase 1 sequencing (S165 invariance diag → S166 canonical hash MVP MinHash/SHA-3-256 + Alembic 017 → S167 claim log scaffolding `bfp_claims` + Alembic 018 → S168 `findings.verified` formalization Alembic 019 → PRE-S169 PQC liboqs-python eval spike → S169 Merkle root). Zero code changes, zero DB writes, no version bump |

## Known issues

- Quick scan timeout (300s) may not be enough for slow Celery
- Some scraper timestamps not parsed (field names vary per API)
- Life timeline sparse (most findings don't include timestamps)
- Fingerprint evolution avatars identical when score stable (by design)
- ~~BUG 4 (deferred): 429 retry~~ — Fixed in Sprint 87 (exponential backoff on all scrapers)
- Intelligence analyzer confidence now severity-based (0.40-0.85), fixed in Sprint 77
- Code leak scrapers require GitHub API rate limit awareness (5 req/min unauth)
- BUG A: Timeline dates — DNS/archive dates may leak into timeline (shows 1999)
- BUG F: GitHub code search requires auth token for higher rate limits
- Graph label cleanup (_profile/_scraper suffix in node labels) — cosmetic
- Discovery: common first names need adding to COMMON_FIRST_NAMES blocklist
- Discovery: JSON-LD Article titles extracted as name leads (filter @type)
- Discovery: session stats show "0 queries" during running (updates on completion only)

## Roadmap

### v1.0 — Nexus 2026 (June)
- [x] 124 scrapers, 26 scanners, 9 intelligence analyzers
- [x] PageRank / Markov chain confidence propagation
- [x] 32x32 pixel art identity avatars with quality gate
- [x] Freemium quick scan (zero friction)
- [x] Plans (Free/Consultant/Enterprise)
- [x] 3-pass pipeline (email → username → name)
- [x] 11-axis digital fingerprint (9 axes at v1.0, +formal_records S145, +network_signature S147)
- [x] Identity-first narrative rewrite
- [x] PDF report export (ReportLab, dark theme, plan-tiered)
- [x] Deep username scan (operator-triggered)
- [x] Code leak monitoring (GitHub Code Search, Gists, Pastebin dumps)
- [x] Behavioral profiling (Developer, Gamer, Creative, Influencer, Privacy-conscious)
- [x] Consumer dashboard preview (/user-preview)
- [x] Remediation engine (prioritized action plan)
- [x] Avatar quality gate (reject identicons/defaults)
- [x] Product manifesto: Ethical OSINT + Green Intelligence + Education First
- [x] Generic deep indicator scan (any indicator type, not just username)
- [x] Cascade scan (cross-type indicator chaining after deep scan)
- [x] Two-phase pipeline (gather all findings before graph/score/profile)
- [x] Per-scraper module attribution (real scraper names in findings)
- [x] 429 exponential backoff retry on all scrapers
- [x] Ratio-based risk level thresholds
- [ ] Graph zoom/pan improvement
- [ ] Corporate intelligence via Hunter.io (domain → employees)

### Sprint 107 series — Phone + crypto identifiers + docs alignment (Apr–May 2026)

- [x] S107a — Phantom module cleanup: 9 phantoms removed/disabled, 4 undocumented live modules documented in CLAUDE.md (v1.1.8)
- [x] S107b — Secondary identifier infrastructure: `secondary_identifiers.py` + `secondary_identifier_enricher.py` services, `finalize_scan` A1.5/A1.6 steps, scraper engine `phone` and `crypto_wallet` input types (v1.1.9)
- [x] S107c — Phone + crypto scrapers: 6 new scrapers seeded disabled (numverify_phone, veriphone_phone, google_phone_dork, blockchain_info_btc, blockchair_wallet, chainabuse_check), key-based JSONB extraction (v1.1.10)
- [x] S107d — Docs sync (partial): 7 files aligned on count, did not catch all references — closure handled in S107e
- [x] S107e — Docs closure + `mastodon_search` dedupe: removed broken duplicate (invalid regex `"accounts":[{`), aligned all docs on 124 scrapers, PRD metadata refreshed, backfilled sprint log entries 107a-d
- [x] S108 — Secondary identifier extraction tests: 8 pytest characterization tests on `_extract_phones` and `_extract_wallets` covering key-based extraction, nested keys, module blacklist, dedupe across findings, BTC/ETH chain detection, and SECONDARY_INPUT_TYPES contract (no production code change)
- [x] S110 — Landing hero identity-layer rewrite: nav exposes Manifesto link, eyebrow reframed "Identity Layer · Free lookup", two intro paragraphs rewritten to identity-as-a-layer thesis (record→layer narrative), chip badges include "Identity layer · alpha", form caption rephrased
- [x] S111 — Manifesto identity-layer thesis section: hero reframed "Identity is a layer of the internet", new Foundation section (4 cards: thesis / what the layer looks like / why this matters now / what xposeTIP is and isn't), bridge intro to four operational pillars, version bumped v2→v3

### Trilogy C→B→A — Play 1 + Play 2 foundation (May 2026)

- [x] S112 — Identity Intelligence Report templates (Sprint C): consulting Markdown generator + client intake form for Play 1 delivery, internal infra only (no public surface), report tiers Quick Profile / Identity Assessment / Deep Investigation (v1.1.11)
- [x] S113 — Public positioning refresh (Sprint B): pricing 4-tier locked on landing (Free/Starter/Team/Enterprise), manifesto BFP-silent, Nexus 2026 references removed, contact@redbird.co.com surface
- [x] S114 — OSS readiness (Sprint A): LICENSE flip MIT→AGPL-3.0, CLA infra (cla-assistant.yml + .github/CLA.md), CONTRIBUTING.md, CODE_OF_CONDUCT.md, SECURITY.md, NOTICE.md, .gitleaks.toml audit clean (0 history leaks)

### Post-trilogy expansion — Identity layer + UI (May 2026)

- [x] S115 — Doc drift closure post-trilogy: CLAUDE.md, README changelog backfill v1.1.1–v1.1.11, SPRINT_LOG sync, CONTRIBUTING duplication resolved (docs/CONTRIBUTING.md → docs/ADDING_A_SCANNER.md), PRD status bump
- [x] S116a — Backend plan config 4-tier alignment: free/consultant/enterprise → free/starter/team/enterprise. Scan limits 25/250/2000/unlimited. New max_seats + feature flags (custom_scrapers, sso, audit_log, multi_tenant). Alembic 012 data migration consultant→starter on workspaces.plan. pdf_generator tier gate updated. Role 'consultant' UNTOUCHED (memberships.role) (v1.1.12)
- [x] S116b — Frontend plan rename + Team tier UI: new dashboard/src/lib/planColors.js (single source of truth), 4 inline duplicate maps removed, Layout/Organization/SystemUsersTab/SystemWorkspacesTab synced, UserPreview upgrade copy "Consultant"→"Starter", new team color #aa66ff (purple), plan cards grid md:grid-cols-3 → lg:grid-cols-4
- [x] S117 — Courtlistener legal scraper (MVP, collection only): US federal court records via Courtlistener REST API v4 (RECAP archive). Token auth, 401 Redis disable flag, 429 retry, conservative name confidence (0.70). PASS2 dispatch block 5b after Interpol. indicator_type=legal_record (does NOT match _PE_EDGE_TYPES — axis math untouched). courtlistener_api_key registered. Source scoring 0.85 (v1.1.13)
- [x] S118 — EU legal scrapers BODACC (FR) + UK Gazette: BODACC via Opendatasoft public dataset (procédures collectives, ventes/cessions, radiations), no auth. UK Gazette via official JSON feed (personal insolvency, bankruptcy, probate, deceased estates), no auth, 10s crawl delay per robots.txt, requires ≥2 name tokens. Pivot from Judilibre (pseudonymized at publication) and UK Insolvency Register (no public API) documented in commit (v1.1.14)
- [x] S119 — Risk Signals UI block on Overview tab: dashboard/src/components/RiskSignalsBlock.jsx, three-column grid (phone #3388ff / crypto #aa66ff / legal #ff8800), self-hides when all empty. Per-column accents match plan colors. Filter logic: data.scraper IN PHONE_SCRAPERS/CRYPTO_SCRAPERS, indicator_type='legal_record'. Closes deferred S108 plan (v1.1.15)
- [x] S120 — Findings tab preset filter chips: dashboard/src/lib/findingFilters.js (shared classifier, single source of truth), RiskSignalsBlock refactored to import from shared lib (no inline arrays). presetFilter state in TargetDetail, handleRiskSignalViewAll resets sev/mod/status + switches tab. FindingsTab renders self-hiding chip row (All / Phone / Crypto / Legal) above existing filter bar. Closes S119 navigation loop (v1.1.16)

### v1.2 stability chain — Doc closure + S122 chain + provenance + telemetry (May 2026)

- [x] S121 — Doc drift closure post-S116→S120: README badges, CLAUDE.md sprint list, SCANNERS.md count reconciliation, landing components stale refs swept (commit 64b57aa)
- [x] S121b — Sweep of remaining drift: DEMO_SCRIPT.md pricing table aligned with S113 4-tier, stale JSX landing references, follow-up to S121 (commit 984c129)
- [x] S122a — QA quick-fixes: sync_avatars script idempotent guard, /targets paginated cap tightened, error renderer fallback, version bump (commit e994b2f)
- [x] S122b — Pre-truncate `result.indicator_value` before SELECT/Identity insert: prevents PostgreSQL VARCHAR overflow on long extraction output (commit 514e4d4)
- [x] S122c — Diagnostic logging + UI banner for failed name resolution: `name_resolution_debug` JSONB on scan, surfaced in TargetDetail banner when aggregator produces no primary_name (commit 1758466)
- [x] S122-clean — Disable 2 Gravatar duplicates (`gravatar_scraper` + `gravatar_json` shadow `gravatar_profile_v2`), fix `pastebin_user` HTML extraction regex (commit 350f173)
- [x] S122-obs — Per-scraper `attempt_log` JSONB on Scan: every scraper dispatch logged with (started_at, ended_at, status, reason), name-resolution diag enrichment (commit 0c8c2d9)
- [x] S122-hk — Move QA reports to `docs/qa/`, add `.gitignore` entries for transient scan artifacts (commit 2cd5f44)
- [x] S122-name — Aggregator tie-break: when 2 sources propose competing names with equal weight, Gravatar email-verified beats heuristic. Emoji regex extends to U+2600 block (misc symbols) for cleanup (commit 0893acc)
- [x] S122e — `name_scraper_engine` module factored out: name-input scrapers (OpenSanctions, Interpol, OpenCorporates, Courtlistener, BODACC, UK Gazette) dispatched via dedicated path with per-scraper timeout + dedup (commit 6544804)
- [x] S122e-holdover — Low-confidence severity + manual-check title for 5 placeholder name-scrapers (preserved as scaffolding pending future activation) (commit 4fb647f)
- [x] Forensic round 2 — Pipeline health validation + scraper inventory snapshot post-S122 chain (commit d2bb4ee, `docs/qa/forensic_round_2_*.md`)
- [x] S123 — Holdover rollback + R1 + R2: R1 = PASS2 finding creation `int → str` coerce for `indicator_value` (NumVerify/Veriphone numeric IDs broke FK), R2 = `courtlistener_search` defensive parse on RECAP shape variants, rollback of 4 placeholder name-scrapers from S122e-holdover (low-signal verdict) (commit 51c5663)
- [x] S124 — Verified provenance UI per finding: `ProvenanceCard.jsx` displays tier badge (verified / cross-verified / unverified), cross-verif source list, first_seen/last_seen timeline. Closes provenance audit gap (commit 705eb3f)
- [x] S125 — Recalculate Profiles retro-rebuilds confidence: System Settings → Recalculate Profiles button now reruns `recalc_finding_confidence` + cross-verif boost over all existing findings, not just future scans (commit 0f30272)
- [x] S126 — `data.match_confidence` as separate ProvenanceCard row: distinct from finding-level confidence — exposes per-source name match score (e.g., OpenSanctions partial vs exact) (commit d7894df)
- [x] S127 — `name_scraper_engine` sequential dispatch post-A3: name-input scrapers fire AFTER A3 name resolution completes (not parallel with Pass 1.5). Fixes no_name_input regression on first scan of unknown identities (commit 9226a37)
- [x] Forensic round 3 — Full S122 → S127 chain validation: 3 Friends targets scanned (Ksontinisarah/abed.belaid/booking.lxb), 7 health checks PASS, all 12 sprints' fixes hold (commit fb394be, `docs/qa/forensic_round_3_*.md`)
- [x] S128 — Telemetry rectification: `scraper_engine._check_found()` returns `tuple[bool, str]` with reasons (`success | explicit_not_found | blocked_403 | implicit_not_found | not_2xx`). `scraper_scanner` classifier reclassifies `explicit_not_found | blocked_403 | implicit_not_found` as `no_data` instead of `error_4xx`. 4 scrapers flipped from error to no_data per scan (bluesky/crunchbase/discogs/wayback). Honest classification surfaced real HN bug → fixed in S129 (commit 77b08f5)
- [x] S129 — Pass 1 dispatch validation symmetry: `is_valid_username()` now gates Pass 1 username dispatch in `scraper_scanner.scan()` (matching Pass 1.5 validation in `username_expander._select_usernames`). Eliminates HN-style 400 errors on multi-dot emails. Broken bucket 12→2 on T2 control (83% reduction). Accepted gap: 1-dot emails still pass through (observe round 4 before tightening) (commit 3c383da)
- [x] S130 — Documentation closure + v1.2.0 stamp: all scraper counts reconciled to authoritative 127 (110 active / 17 disabled), sprint count 114+ → 129+, version v1.1.16 → v1.2.0 across README/CLAUDE/SCANNERS/PRD/footers, DEMO_SCRIPT.md archived (Nexus narrative dead) and rewritten for Play 1 + Play 2 framing, ScraperBreakdown.jsx category table reconciled with new Financial row + Public Exposure recount, SPRINT_LOG backfill S121-S129
- [x] S183 — Crypto scraper catalog expansion 3→15: 12 new chain-aware crypto scrapers seeded (Etherscan/BscScan/Polygonscan/Snowtrace/Arbiscan/Optimistic/Basescan/Tronscan/SolanaFM/Mempool BTC/Mempool LTC/Mempool DOGE), each tagged with `accepts_chain` array (Alembic 023 prereq). Foundation for S185 dispatch gate (commit d2379d2)
- [x] S185 — Crypto chain-shape dispatch gate at A1.6: NEW `scrapers.accepts_chain` ARRAY(Text) column (Alembic 023 + GIN index `idx_scrapers_accepts_chain`). Secondary enricher gates crypto scrapers by detected chain shape (BTC bech32/legacy vs ETH 0x… vs SOL base58 vs TRX T…) instead of firing the full 15-scraper fleet against every wallet. Reduces wasted dispatches ~70% on typical scans. Worker container restart required to pick up new gate logic (worker import cache, not API) (commit de5095d)
- [x] S187 — Legal expansion P0: SEC EDGAR + Companies House UK. NEW `sec_edgar_search` (US public-company officer/director filings via EDGAR full-text) and `companies_house_uk` (UK officer/PSC search via Companies House API, Fernet-decrypted workspace API key). Both wired through name_scraper_engine Pass 2 dispatch with `identity_type='legal_record'`, feed S145 `formal_records` axis. Brings legal record coverage to US (federal courts + SEC) + FR (BODACC) + UK (Gazette + Companies House) (commit 940c922)
- [x] S188 — `/api/v1/targets/all` superadmin endpoint + Targets adaptive refresh polling. NEW `GET /targets/all` (requires `superadmin`, returns every target across every workspace joined with workspace name + plan). Frontend Targets page switches to adaptive polling (3s when any target has `cascade_state` in flight, 10s otherwise) reducing background fetch cost ~3× at workspace idle. Route must precede `/{target_id}` UUID path in FastAPI router (commit 915a165)
- [x] S189 — Doc drift closure post-S183/S185/S187/S188 + BFPStatus API-driven refactor + v1.6.13 → v1.6.14: scraper count 127→139, scanner count 26→27, fingerprint axes 9→11 reconciled across README/CLAUDE/SCANNERS/DEMO_SCRIPT/ARCHITECTURE + 11 JSX components (landing, architecture, BFP, DiscoveredTab, FingerprintRadar). Active/disabled split corrected from spec's 113/26 to live DB ground truth 116/23. `bfp.py` `/stats` endpoint extended with `_PLATFORM_INVENTORY` constants (scrapers/scanners/analyzers/axes/version) so BFPStatus.jsx renders live counts instead of hardcoded strings — single source of truth at API layer. Stale `DEMO_SCRIPT_NEXUS_v1.md` deleted (Nexus narrative ended at S130)
- [x] S199 — Fix dispatch gap: `scraper_engine` appended to `DEFAULT_QUICK_MODULES` (`api/routers/scans.py`) and `QUICK_MODULES` (`api/main.py` public quick-scan flow). scraper_engine is the dispatcher for all 170 data-driven scrapers in `seed_scrapers.py` — it was registered in `SCANNER_REGISTRY` but never added to the default module lists. Result: every default scan since the constants were introduced was structurally skipping the entire PASS1 data-driven dispatch, including S197 Maigret picks (20) and S198 HN/OTX (2). The bug stayed hidden because Phase A.5 discovery cascade DOES dispatch scraper_engine per discovered lead, surfacing some findings that gave the appearance of working dispatch. Free-tier behavior unchanged (cap=7 modules, never reaches positions 8-9). Paid tiers gain the dispatch. Public quick-scan now has 6 modules (under cap). Workspace overrides (`workspace.settings.default_modules`) not touched — Friends ws still has custom list and needs manual fix if smoke re-run touches it. Found during S196+S197+S198 multi-workspace smoke prep when Crypto and Dataminr default scans showed zero findings from the 25 new scrapers
- [x] S200 — Doc/site drift closure post-S196-S199: 139→170 scrapers, 116→145 active, 23→25 disabled, v1.6.14/v1.6.17→v1.6.18. 14 files refreshed (api/routers/bfp.py inventory + README + CLAUDE + ARCHITECTURE + this SPRINT_LOG + 6 landing JSX/JS + 2 architecture JSX + 1 BFP JSX). Category breakdown recomputed in ScraperBreakdown.jsx (Social 50→70 from S197 Maigret +20, Metadata 15→19 from S196 phone dorks +3 + S198 HN +1, Identity 6→8 from S196 API validators +2, Public Exposure 10→15 from S198 academic quartet +4 + OTX +1; other 7 categories unchanged). Active/disabled split 116/23→145/25 (83%→85% activation). `_PLATFORM_INVENTORY` bumped v1.6.17→v1.6.18 to keep code + doc-facing version strings strictly aligned. BFPStatus.jsx not touched — already API-driven via `/bfp/stats` per S189 design, auto-updates from `_PLATFORM_INVENTORY` at runtime. Bonus fix: docs/ARCHITECTURE.md L186 carried "120 fixed scrapers" (pre-historic stale ref pre-dating S130 v1.2.0) — corrected to 170 in same pass. Doc-sync exception per rule #3, precedent S189. Pipeline impact: zero (pure text refresh).
- [x] S201 — Session-artifact cleanup. Committed `docs/BFP_AXES_AUDIT.md` in-place (companion to `docs/BFP_SPEC_v0.md`, explicitly cited in `docs/qa/bfp_pre_impl_audit_2026-05-23.md` lines 253+261 as "should be committed alongside BFP_SPEC_v0.md but not yet in repo" — tracked-doc reference now resolves). Archived 7 other untracked session-leftover files to `docs/archive/2026-05-24/` preserving relative paths (2 docs/ root-level files + 4 docs/diag/ fingerprint calibration snapshots + 1 repo-root rescan_kpmg_dataminr.sh helper). Spec assumed 33 untracked (PM session's stale local view); actual was 8 (1 commit-in-place + 7 archive). Working tree clean post-merge. Zero behavior change. Pure repo hygiene, doc-sync exception per rule #3.
- [x] S202 — Landing kill-darlings step 1/3 progressive refonte (per R&D 2026-05-24 option B). Removed 3 darling components: `LiveCounter.jsx` (hardcoded counter theater, anti-trust on an "honest OSINT" product — start value 4_237_891_442 reset every reload), `FinalCTA.jsx` (duplicate of Hero scan form below pricing, fragments primary CTA), `TechStack.jsx` (info-dump tech logos row, content already covered by /architecture page via `TechStackSection.jsx`). Edited 2 consumers: `Landing.jsx` (2 imports + 2 JSX usages removed), `ProblemSection.jsx` (LiveCounter import + the `mt-20 text-center` breach-counter block removed). Bonus drift fix in `ProblemSection.jsx`: `'9' label: 'behavioral dimensions'` → `'11'` aligns with S145 formal_records + S147 network_signature making BFP an 11-axis radar. Net delta: -3 deleted, 2 edited. Pipeline impact: zero (UI only, no API/DB touched). Step 2/3 (Hero split-screen refonte) + Step 3/3 (NEW Inversion section per locked vision May 22) sequenced as S203/S204.
- [x] S203 — Hero split-screen refonte + composite hero asset (step 2/3 progressive refonte). `HeroSection.jsx` refactored: left column trimmed from 4 body paragraphs to 1 tight paragraph ("xposeTIP treats identity as a layer. Behavioral. Persistent. Foundational. Type your email below — see what the internet knows about you."), 3 outdated tag pills replaced with 3 locked R&D trust signals (`170 OSINT sources` / `11-axis behavioral fingerprint` / `🇱🇺 Made in Luxembourg`), redundant trailing paragraph removed; right column replaced the `DEMO_AVATARS` floating-animation grid with a single composite hero `<img>` referencing `/hero/identity-report-composite.webp` (subtle ambient glow + eager fetchpriority). Deleted: `MockPreview.jsx` (replaced by Hero right panel), `TrustBar.jsx` (3 of 6 signals now inline in Hero, other 3 dropped). `Landing.jsx`: 2 imports + 2 JSX usages removed. Composite asset captured live via Playwright on Quentin/gnienlnaha (seeded synthetic test target — Satoshi BTC + fake +33 phone — innocuous source), DOM-injected anonymization replaced any visible email/handle/wallet/phone with stand-ins (`analyst@example.com` / `analyst` / `1BTC…AnonAddress…` / `+33 6 XX XX XX XX`) before full-page screenshot, then cropped to top 870px (avatar + scores + accounts + Risk Signals block) and resized 720×499 webp q92 (~27KB). Single-source for now; spec's 3-target stitched composite deferred — operator can replace with multi-source via the documented Playwright+PIL capture flow when picks land. Net delta: -2 deleted, 2 edited, +1 asset. Pipeline impact: zero. Step 3/3 (NEW Inversion section per locked vision May 22, Framer Motion animated SVG per R&D Q1) sequenced as S204.
- [x] S204 — NEW InversionSection — step 3/3 final of progressive refonte (closes trilogy S202→S203→S204). Materializes the foundational vision locked 2026-05-22 ("The internet knows who you are. Everyone knows who you are. Except you.") as a dedicated section with Framer Motion animated SVG. State 1 (asymmetry, #ff5588): 6 extraction arrows pointing INTO central "you" subject from 6 actor archetypes (Data brokers, Social platforms, Surveillance, Breaches, Search engines, Public records). State 2 (symmetry, #00ff88): BFP ring emerges around subject + 6 outgoing dashed arrows pointing OUT to same actors — subject now sees what they see. Sequencing: extraction (0-0.7s) → subject + "you" label (0.5-0.7s) → BFP ring (1.2-2.1s) → symmetry arrows (1.6-2.5s) → headline + subhead fade in. All animations whileInView once-only. 3 colors lock: #00ff88 / #ff5588 / #666. Background grid raccord with Hero. Headline EN-only per Q2 lock. Installed framer-motion@^11 (React 18 compatible). Landing.jsx: InversionSection inserted between Hero and ProblemSection. ProblemSection delete deferred to polish sprint (avoids mid-state content gap). Net delta: +1 dep, +1 NEW component (~155 lines), 2 imports + 1 JSX line edited. Pipeline impact: zero. Polish sprint sequenced based on full-screen visual review per Nabil's "s204 puis on polish l ux apres".
- [x] S205 — NEW BFPLayer section on `/architecture` (Y1/3 progressive refonte). Closes BFP gap on `/architecture` per R&D 2026-05-24 option Y — before this sprint BFP appeared only as a trailing sentence in StageSimilarity ("This is the foundation for the Behavioral Fingerprint Protocol —" cut mid-air), no diagram, no section, no pointer to `/bfp` despite BFP being one of 3 strategic pillars. NEW `BFPLayer.jsx` inserted between StageDiscovery and ScraperBreakdown : medium narrative (2 paragraphs distinguishing pipeline "what exists" from BFP "do points converge" + 4 locked properties: 11-axis hash · cross-source convergence · SHA-3 Merkle audit RFC 6962 · PQC sigs SPHINCS+/ML-DSA/ML-KEM + pointage `/bfp`) + 1 system-layers diagram (Inputs → OSINT pipeline → split into Reports neutral + BFP layer highlighted #00ff88 with 8-step framer-motion whileInView once-only sequencing). Style raccord exact Inversion S204 : 4-color palette (#00ff88/#3388ff/#888/grays), viewBox 600×400 (vs current Diagrams.jsx cramped 400×280), fontSize ≥11 throughout, section bg #0d0d14 for alternate-row rhythm. 3 files (1 NEW component ~165 lines + 1 import-edit in Architecture.jsx + 1 doc row). Pipeline impact: zero. Y2 (unify Diagrams.jsx 8-function visual language + ScraperBreakdown reskinning + Stages.jsx 408-LOC refactor to map-on-array) and Y3 (NEW hero system diagram replacing current text+stats hero on /architecture) sequenced.
- [x] S209 — Maigret tranche 2: +4 username scrapers (Axe D, R&D 2026-05-24 pick D). 30 candidates curated from Maigret upstream (MIT, `soxoj/maigret/main/maigret/resources/data.json` — 3,158 sites indexed), live-validated per S197 protocol (positive `nabz0r`/`torvalds` + negative gibberish `xpose_t2_nonexistent_uname_zzz9k7`, both HTTP-tested with realistic Safari/macOS UA, refined bot_wall detection — only fires on status 403/503 OR body <8KB with explicit "just a moment"/"cf-challenge"/"g-recaptcha"/"perimeterx"-class markers to avoid false positives on legitimate Cloudflare-CDN pages). **4/30 passed** (13% — far below spec's expected 80-90% pass rate). The 4 survivors all rely on status differential (positive=200, negative=404) with empty `success_indicator` falling through `scraper_engine.py:222-224` `status_code==200 = found` path. **Shipped (4)**: `deviantart_profile` (deviantart.com), `scratch_profile` (scratch.mit.edu/users/), `couchsurfing_profile` (couchsurfing.com/people/), `patreon_profile` (patreon.com/). **Dropped (26)**: 14 Cloudflare-403 walls (leetcode, codeforces, codepen, hackerearth, vsco, artstation, mydramalist, tellonym, fiverr + others), 6 stale URLs returning 404 for both (observablehq, atcoder, exercism, askfm, lobste.rs, tilde.news + maigret has wrong forum domain for sketchfab), 5 SPAs serving identical shell HTML for any username (glitch, 500px, indiehackers, founderio, audiomack), 1 returning 202-PWA (sketchfab.com main site), 1 returning 401 (unsplash), 1 401 (newgrounds — DNS issue from sandbox). Doc-sync sweep per S189/S200 precedent: BFP `_PLATFORM_INVENTORY` 170→174 scrapers / 145→149 active / `v1.6.19`→`v1.6.20`; README badge + 6 inline refs; CLAUDE.md `## Current version` v1.6.18→v1.6.20 (catch-up from S208 v1.6.19) + recent-sprints line + key files line; ARCHITECTURE.md 2 refs; SCANNERS.md 4 refs incl. Social category 70→74; ScraperBreakdown.jsx Social 70→74 + total 170→174 + active 145→149 + h2 title; Architecture.jsx hero stat banner 170→174; 6 landing/architecture JSX refs (HeroSection / constants / TwoWaysSection / PipelineSection / DesignPrinciples / Stages ×2 / HeroDiagram / BFPLayer / RoadmapSection); 3 cross-page refs (BFPArchitecture.jsx, Manifesto.jsx, DiscoveredTab.jsx). Pipeline impact: zero behaviour change for existing scans — `scraper_engine` `DEFAULT_QUICK_MODULES` dispatch already covers the 4 new entries (post-S199). Active username scrapers 92 → 96 (S208 `MAX_SCRAPERS=92` cap is actually now `MAX_SCRAPERS=92` < 96 — minor gap; ship + log for tranche 3 to revisit). 1 code file (`scripts/seed_scrapers.py` +4 entries) + 14 doc/JSX files + 1 SPRINT_LOG row. Zero migration, zero new dep. Doc-sync exception per rule #3, precedent S189/S200. Tranche 3 sequenced for next mass-add — high drop rate confirms Cloudflare-walled targets need either residential proxy or alternative endpoints (API instead of HTML scraping).
- [x] S208 — Axe A: Deep Scan + Pass 1.5 coverage quick wins (R&D 2026-05-24 picks R1+R3+R6). **R1** `phone` + `crypto_wallet` + `first_name` added to `ALLOWED_TYPES` (`api/routers/targets.py:557`), `_map_indicator_to_input_types` (`api/services/layer4/username_expander.py:684`), and frontend `SCANNABLE_TYPES` (`dashboard/src/pages/tabs/FindingsTab.jsx:217`). Critical companion fix: `_build_format_kwargs` (l709) extended with `phone` / `phone_clean` / `crypto_address` keys matching `scraper_engine.py:65-68` canonical form (`phone.replace("+", "")` for `phone_clean`, raw value for `crypto_address`) — without this the `_SafeFormatDict` fallback would leave `{phone}` / `{crypto_address}` literal in URLs, triggering the `template_incomplete` guard and silently dropping every dispatch. Caps: `INDICATOR_TIMEOUT` adds `phone: 120` + `crypto_wallet: 180`; `INDICATOR_MAX_SCRAPERS` adds `phone: 10` + `crypto_wallet: 20`. **R3** `INDICATOR_MAX_SCRAPERS["username"]` 80→100 and `DEEP_MAX_SCRAPERS` 80→100 — unlocks the 12 active username scrapers previously beyond the cap on operator-triggered deep scans. **R6** Pass 1.5 caps: `MAX_USERNAMES` 3→5, `MAX_SCRAPERS` 50→92 (covers all 92 active username scrapers), `TOTAL_TIMEOUT` 120→180s. Operator now sees Deep Scan buttons in Risk Signals block on phone/wallet rows (previously phantom — buttons greyed for unsupported types). Pipeline impact: auto Pass 1.5 dispatches up to 5×92 = 460 scrapes per target (was 3×50 = 150), within 180s budget vs. 120s. 4 files: 1 router, 1 service module, 1 JSX, 1 SPRINT_LOG row. Zero migration, zero new dep. Doc-sync exception per rule #3 — version bump v1.6.18→v1.6.19 in `_PLATFORM_INVENTORY` (`api/routers/bfp.py`) for landing/architecture freshness.
- [x] S207 — NEW HeroDiagram on `/architecture` (Y3/3 closes refonte trilogy per R&D 2026-05-24 option Y). Animated system-level diagram inserted between Hero text and 5-stat banner. **5 layers top-to-bottom** : Inputs (email · domain · name · hash) → OSINT pipeline #3388ff accent (170 sources · 11 stages · 9 L4 analyzers + full 11-stage list) → Identity graph (Postgres + pgvector) → split into Reports (5-page PDF · JSON API · Plays 1/2/3 · Operator dashboard, left, neutral) + BFP layer (11-axis hash · Append-only log · PQC sigs · Merkle audit, right, highlighted #00ff88). **Animation sequence ~3.5s** : 0.2s Inputs slides in → 0.5s arrow → 0.7s OSINT (blue accent) → 1.0s arrow → 1.2s Identity graph → 1.5-1.7s split arrows (green to BFP, gray to Reports) → 1.9s Reports → 2.1s BFP scale-in → 2.8s subtle BFP pulse (one repeat to draw the eye). framer-motion `whileInView` once-only with `viewport={{ once: true, amount: 0.3 }}` on all 15 motion.* wrappers. Style raccord exact Inversion S204 / BFPLayer S205 / Diagrams S206 : palette 4 colors (#00ff88/#3388ff/#888/#aaa/#666 + #1e1e2e/#13131c bg), viewBox 800×600 (wider than BFPLayer 600×400 because 5 layers + split), fontSize 11-15 (NEVER below 11), strokeWidth 1.5 constant (2 on emphasis only — OSINT + BFP outlines + BFP arrow). Background grid raccord avec Hero/Inversion (#00ff88 opacity-[0.02], 60px gridsize). 3 files (1 NEW `HeroDiagram.jsx` ~140 lines + 1 edit `Architecture.jsx` Hero section restructure outer `max-w-3xl` → `max-w-5xl` to accommodate diagram width + 1 doc row). **Closes Y-trilogy** (Y1 BFPLayer S205 + Y2 visual unify S206 + Y3 hero diagram S207). Hero h1 + tagline + 5-stat banner (170 · 11 · 5.4B · 11 · 0 GPU) preserved verbatim — only outer container max-width changed and tagline `mb-10` → `mb-4` (tighter spacing because diagram now follows). Cross-step gates : vite build clean (3060 modules, 0 errors, built in 6.17s), brackets balanced (Architecture 18/18, HeroDiagram 189/189), palette + viewBox + fontSize compliance check passes, insertion order h1(29) → HeroDiagram(39) → stats(41). S208 polish optional (Stage 08 GeoMapDiagram abstract diagnostic + per-stage number color rainbow at 15% opacity sweep). Pipeline impact: zero.
- [x] S206 — `/architecture` visual language unification (Y2/3 progressive refonte). 3 commit-separable steps closed in sequence : (1/3 `a0ba600`) **Diagrams.jsx** — 8 diagram functions (CollectDiagram / GraphDiagram / PropagateDiagram / ScoreDiagram / GeoMapDiagram / CascadeDiagram / SimilarityDiagram / DiscoveryDiagram) refactored to BFPLayer S205 standards : viewBox 400×280 → 600×400 (all 8 — cramped → readable), fontSize 7-9 → 11-14, rainbow 9+ colors → 4 semantic mappings (#00ff88 identity / #3388ff data-flow / #ff5588 risk / #888 neutral), strokeWidth normalized to 1.5, every element wrapped in `motion.*` with shared `fadeIn`/`popIn`/`drawLine` helpers + framer-motion `whileInView` once-only on `viewport={{ once: true, amount: 0.3 }}`. (2/3 `d2194d4`) **ScraperBreakdown.jsx** — 11 distinct hex pastilles unified to 4-color palette proposal A semantic buckets : Social/People Search/Identity/Social Account #00ff88 · Metadata/Archive #3388ff · Public Exposure/Breach/Code Leak #ff5588 · Gaming/Financial #888. Categories now distinguished by typography + count number prominence, not by rainbow noise. Structure preserved (11 rows + Total + Active/Disabled split rows unchanged). (3/3 `365d8c9`) **Stages.jsx 408 LOC → 378 LOC array-driven** — 11 named-export quasi-identical stage components (`StageCollect`, `StageGraph`, `StagePropagate`, `StageScore`, `StageIdentify`, `StageExpose`, `StageMeasure`, `StageLocate`, `StageCascade`, `StageSimilarity`, `StageDiscovery`) collapsed to a single `STAGES_DATA` array of 11 entries consumed by one generic `<StageBlock />` + `<Paragraph />` tone dispatcher (lead / detail / detail-mono / footnote / centered / centered-subtle + 2 inline-block tones for Stage 02 graph-edges and Stage 04 dual-score), plus 3 special diagram blocks preserved verbatim (`IdentifyAvatarsBlock` for Stage 05 GenerativeAvatar grid, `EnrichLayersBlock` for Stage 06 4-layer cards, `FingerprintAxesBlock` for Stage 07 9-axis full-width grid), plus `layout: 'full' | 'split'` switch (only Stage 07 is full-width). `Architecture.jsx` 11 named imports → 1 default import + 11 JSX children → `<Stages demoSeed={demoSeed} />`. VERBATIM content preservation enforced via token-count gate (A1.5, A1.6, A3.5, Pass 1.5, Pass 2, Alembic 013, 0.85, 0.70, first_detected, target_similarities, 11-axis ×3, cosine all present in final). Per-stage number color preserved (out-of-palette colors #ffcc00, #ff8800, #cc88ff, #88aaff, #ffaa55 kept for visual rhythm — only Diagrams + ScraperBreakdown pastilles unified). Cross-step gates : vite build clean (3060 modules, 0 errors), brackets balanced (Diagrams + Stages + Architecture), single import in Architecture, no old `Stage*` named refs remaining. Y3 (NEW hero system diagram replacing current text+stats hero) sequenced as next refonte sprint. Pipeline impact: zero (UI-only).

### v1.1 — Post-Nexus (July-August)
- [x] Phase C Web Discovery (fingerprint-driven Google dorking)
- [x] 6 content extractors (rel=me, JSON-LD, social links, email, meta, username)
- [x] Quality gate (5-layer anti-noise: nav strip, relevance, LinkedIn penalty, geo, page relevance)
- [x] Discovery leads workflow (dismiss/undo, filter by status)
- [x] Discovered tab in UI with launch button + polling
- [ ] D3 discovery tree visualization (Sprint G)
- [ ] Ingest workflow: Enrich target / New linked target (Sprint H)
- [ ] Target links navigation (Sprint I)
- [ ] Consumer auth flow (self-service scan + save)
- [ ] Score evolution tracking (before/after comparison)
- [ ] Reverse image search (TinEye, cross-platform photo matching)
- [ ] Corporate/LDAP scrapers (O365 enumeration, Azure AD tenant, GitHub org)
- [ ] Domain-wide scan (all employees of company)
- [ ] Public API — behavioral fingerprints as structured intelligence
- [ ] Webhook notifications
- [ ] On-premise deployment guide
- [ ] Consent gate enforcement (scan authorization flow)
- [ ] Right-to-delete (full purge endpoint + UI)
- [ ] Finding explanations (plain-language "why this matters" per finding type)

### v1.2 — Enterprise (Q4 2026)
- [ ] OAuth audit (Google/Microsoft third-party app access)
- [ ] Dark web monitoring (Tor paste sites, .onion indexing)
- [ ] Scheduled recurring scans with delta reports
- [ ] Compliance reports (NIS2, DORA, AI Act)
- [ ] Multi-language (FR, DE, LU)
- [ ] Email alerts (new breach detected for monitored identities)
- [ ] Carbon footprint dashboard (scan cost in watts + CO2 vs industry average)
- [ ] Educational guides per finding category (interactive walkthroughs)
- [ ] Compliance: consent audit trail (who authorized which scan, when)

### v2.0 — Platform (2027)
- [ ] Marketplace (community scrapers and analyzers)
- [ ] Plugin API (custom analyzers, webhook integrations)
- [ ] Team collaboration (shared investigations, annotations)
- [ ] Mobile app (React Native)
- [ ] Real-time paste monitoring (streaming pastebin/GitHub events)
